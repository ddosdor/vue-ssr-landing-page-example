{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    },
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "serverFiles": [
          "server/**"
        ],
        "internalServer": true,
        "generateStaticRoutes": true
      } 
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.js" }
  ]
}