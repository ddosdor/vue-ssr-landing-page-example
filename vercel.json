{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    },
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "serverFiles": [
          "server/**",
          "server/ssr-manifest.json",
          "client/**"
        ],
        "internalServer": true
      } 
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.js" }
  ]
}